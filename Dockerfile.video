FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/*
RUN pip install fastapi uvicorn python-multipart edge-tts httpx
COPY video_gen.py /app/
EXPOSE 8003
CMD ["python", "/app/video_gen.py"]
